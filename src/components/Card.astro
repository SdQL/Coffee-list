---
export interface Props {
    coffee: {
        name: string,
        image: string,
        price: string,
        rating: number,
        votes: number,
        popular: boolean,
        available: boolean
    }
}

const { name, price, image, rating, votes, popular, available } = Astro.props.coffee;


---


<div class="max-w-sm bg-white border border-gray-200 rounded-lg shadow dark:bg-gray-800 dark:border-gray-700">
    <a href="#">
        <img class="rounded-t-lg" src={image} alt="" />
    </a>
    <div class="p-5">
        <a href="#">
            <h5 class="mb-2 text-sm font-bold tracking-tight text-gray-900 dark:text-white">{name}<span class="ml-5 mr-5 bg-white text-black text-xs p-1 rounded-lg">{price}</span></h5> 
        </a>
        
    </div>
</div>
